[!] update; [] undone; [x] done; [-] discard
------------------------------------------------------
[!] addGuest via: guest from previous events
[!] search for guest by qrcode (contactSearch)
[!] password Registration link with a general password
[!] order message component
[!] Add items to guest data
[!] Create guest groups


[] allow multiple pictures in communication
[] small note attached to pictures in communication
[] convert phoneNumber to international format
[] do notification page
[] Invitation link tracking
[] make sure selected contact contains phoneNumber or email
[] padding and width problem
[] make sure poll title is unique by verifing with previous polls
[] add and use image from static resources
[-] fiterOrganizer [all, selected, byPermmision]
[] create document component
[-] create select Organizer page
[] assign more Organizers to task and add lead
[] chat for Organizer (filter who to chat with, sent [text, image, and document, voiceRecording])

//camera
ImagePicker.openCamera({
  width: 300,
  height: 400,
  cropping: true,
  mediaType: 'photo'
}).then(image => {
  console.log(image);
});

//multiple image
ImagePicker.openPicker({
  multiple: true,
  mediaType: 'photo'
}).then(images => {
  console.log(images);
});

//single with cropping
ImagePicker.openPicker({
  width: 300,
  height: 400,
  cropping: true,
  mediaType: 'photo'
}).then(image => {
  console.log(image);
});