import { } from '../types';

let init = [];

function tablesReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default tablesReducer;