import { } from '../types';


let init = [];


function pollsReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default pollsReducer;