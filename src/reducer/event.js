import { } from '../types';

let init = {

};

function eventReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default eventReducer;