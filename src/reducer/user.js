import { } from '../types';

let init = {

};

function userReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default userReducer;