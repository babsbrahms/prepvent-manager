import { } from '../types';

let init = {
    name: '',
    uid: ''
};

function userReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default userReducer;