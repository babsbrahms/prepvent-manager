import { } from '../types';

let init = {
    name: 'Olayinka Ibrahim',
    uid: '1212j232kjui'
};

function userReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default userReducer;