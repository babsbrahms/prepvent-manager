import { } from '../types';

let init = {
    name: 'Olayinka Ibrahim',
    uid: '1212j232kjui',
    countryCode: '+234',
    phoneNumber: '',
    email: ''
};

function userReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default userReducer;