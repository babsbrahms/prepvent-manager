import { } from '../types';

let init = [];

function notificationReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default notificationReducer;