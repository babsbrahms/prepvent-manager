import { } from '../types';

let init = [];

function organizerReducer (state = init, action) {
    switch (action.type) {

        default:
            return state;
    }
}

export default organizerReducer;